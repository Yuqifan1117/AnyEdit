import os

os.makedirs("./checkpoints/flitering_model/blip-t5", exist_ok=True)

os.makedirs("./checkpoints/foundation_models/groundingDINO", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/instruct-pix2pix", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/ultraedit", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/omost-llama-3-8b-4bits", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/RealVisXL_V4", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/sd3-med", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/flux", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/sd-inpaint", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/stable-diffusion-v1-5", exist_ok=True)
os.makedirs("./checkpoints/foundation_models/stable-diffusion-xl-base-1.0", exist_ok=True)

os.makedirs("./checkpoints/material_transfer/controlnet-canny-sdxl-1.0", exist_ok=True)
os.makedirs("./checkpoints/material_transfer/controlnet-depth-sdxl-1.0", exist_ok=True)
os.makedirs("./checkpoints/material_transfer/ip_adapter", exist_ok=True)
os.makedirs("./checkpoints/material_transfer/stable-diffusion-2-1", exist_ok=True)

os.makedirs("./checkpoints/visual_reference/depth_anything_v2", exist_ok=True)
os.makedirs("./checkpoints/visual_reference/vition", exist_ok=True)

os.makedirs("./checkpoints/llama3-8b", exist_ok=True)